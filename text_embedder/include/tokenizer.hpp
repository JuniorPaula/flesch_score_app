#ifndef TOKENIZER_H
#define TOKENIZER_H

#include <string>
#include <vector>

std::vector<std::string> tokenizer(const std::string& text);
std::string normalize(const std::string& input);

#endif // TOKENIZER_H
